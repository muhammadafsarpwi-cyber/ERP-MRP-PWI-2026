# Optional. When unset, the frontend calls the API at
# http://<browser-hostname>:3001/api/v1 so it works from localhost
# and from LAN addresses (e.g. http://192.168.100.9:3000).
# Set only when the API runs on a different host than the frontend.
# REACT_APP_API_URL=http://localhost:3001/api/v1
